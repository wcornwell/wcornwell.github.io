*.DS_Store
**/.DS_Store
.hugo_build.lock
/docs/
docs/
public/
_site/
Gemfile.lock
resources/
__pycache__/
.claude/settings.local.json
# stray working files dropped in the repo root (grant PDFs etc.);
# PDFs meant for the site live under static/ and are still tracked
/*.pdf
# local abstract cache for scripts/cluster_abstracts.py -- publisher copyright
# (Elsevier/Scopus especially), and this repo is public. Never commit it.
data/abstracts.local.json
